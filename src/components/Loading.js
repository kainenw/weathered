import React from "react";

export function Loading(){

    console.log('load')
    /* function interval(str){
        setInterval(str, 3000)
    }

    function loading(){
        interval('loading.');
        setTimeout(interval('Loading..'), 1000);
        setTimeout(interval('Loading...'), 2000);
    }; */
    
    return <h3>Loading</h3>
}